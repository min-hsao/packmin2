<%= turbo_stream_from "packing_list_#{@packing_list.id}" %>

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-6">
    <div>
      <h1 class="text-2xl font-bold text-gray-900"><%= @packing_list.title %></h1>
      <p class="text-gray-600"><%= @packing_list.total_days %> days • <%= @packing_list.destination_names %></p>
    </div>
    
    <div class="flex gap-2 mt-4 sm:mt-0">
      <% if @packing_list.status == 'complete' %>
        <%= link_to regenerate_packing_list_path(@packing_list), 
            method: :post,
            class: "px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50" do %>
          🔄 Regenerate
        <% end %>
      <% end %>
      
      <%= link_to @packing_list, 
          method: :delete,
          data: { confirm: "Are you sure you want to delete this packing list?" },
          class: "px-4 py-2 border border-red-300 text-red-600 rounded-lg hover:bg-red-50" do %>
        🗑️ Delete
      <% end %>
    </div>
  </div>
  
  <div id="packing_list_content">
    <%= render 'content', packing_list: @packing_list %>
  </div>
</div>
