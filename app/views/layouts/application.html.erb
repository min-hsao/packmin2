<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <title>PackMin2 - Smart Packing Lists</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-gray-50">
    <div class="min-h-full">
      <% if user_signed_in? %>
        <!-- Navigation -->
        <nav class="bg-white shadow-sm border-b border-gray-200">
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
              <div class="flex items-center">
                <%= link_to dashboard_path, class: "flex items-center" do %>
                  <span class="text-2xl">🎒</span>
                  <span class="ml-2 text-xl font-bold text-gray-900 hidden sm:block">PackMin2</span>
                <% end %>
                
                <div class="hidden md:ml-8 md:flex md:space-x-4">
                  <%= link_to "Dashboard", dashboard_path, class: "px-3 py-2 rounded-md text-sm font-medium #{request.path == dashboard_path ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:text-gray-900'}" %>
                  <%= link_to "Packing Lists", packing_lists_path, class: "px-3 py-2 rounded-md text-sm font-medium #{controller_name == 'packing_lists' ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:text-gray-900'}" %>
                  <%= link_to "Locations", saved_locations_path, class: "px-3 py-2 rounded-md text-sm font-medium #{controller_name == 'saved_locations' ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:text-gray-900'}" %>
                  <%= link_to "Items", custom_items_path, class: "px-3 py-2 rounded-md text-sm font-medium #{controller_name == 'custom_items' ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:text-gray-900'}" %>
                </div>
              </div>
              
              <div class="flex items-center space-x-3">
                <% if current_user.avatar_url.present? %>
                  <%= image_tag current_user.avatar_url, class: "h-8 w-8 rounded-full", alt: current_user.display_name %>
                <% else %>
                  <div class="h-8 w-8 rounded-full bg-blue-500 flex items-center justify-center text-white text-sm font-medium">
                    <%= current_user.display_name[0].upcase %>
                  </div>
                <% end %>
                
                <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-sm text-gray-600 hover:text-gray-900 hidden sm:block" %>
              </div>
            </div>
          </div>
          
          <!-- Mobile nav -->
          <div class="md:hidden border-t border-gray-200">
            <div class="flex justify-around py-2">
              <%= link_to dashboard_path, class: "flex flex-col items-center p-2 #{request.path == dashboard_path ? 'text-blue-600' : 'text-gray-600'}" do %>
                <span class="text-lg">📊</span>
                <span class="text-xs mt-1">Home</span>
              <% end %>
              <%= link_to packing_lists_path, class: "flex flex-col items-center p-2 #{controller_name == 'packing_lists' ? 'text-blue-600' : 'text-gray-600'}" do %>
                <span class="text-lg">📋</span>
                <span class="text-xs mt-1">Lists</span>
              <% end %>
              <%= link_to saved_locations_path, class: "flex flex-col items-center p-2 #{controller_name == 'saved_locations' ? 'text-blue-600' : 'text-gray-600'}" do %>
                <span class="text-lg">📍</span>
                <span class="text-xs mt-1">Places</span>
              <% end %>
              <%= link_to custom_items_path, class: "flex flex-col items-center p-2 #{controller_name == 'custom_items' ? 'text-blue-600' : 'text-gray-600'}" do %>
                <span class="text-lg">🎒</span>
                <span class="text-xs mt-1">Items</span>
              <% end %>
            </div>
          </div>
        </nav>
      <% end %>

      <!-- Flash messages -->
      <% if notice.present? %>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
          <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg flex justify-between items-center">
            <span><%= notice %></span>
            <button type="button" onclick="this.parentElement.remove()" class="text-green-700 hover:text-green-900">&times;</button>
          </div>
        </div>
      <% end %>
      
      <% if alert.present? %>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
          <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg flex justify-between items-center">
            <span><%= alert %></span>
            <button type="button" onclick="this.parentElement.remove()" class="text-red-700 hover:text-red-900">&times;</button>
          </div>
        </div>
      <% end %>

      <!-- Main content -->
      <main class="pb-20 md:pb-8">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
